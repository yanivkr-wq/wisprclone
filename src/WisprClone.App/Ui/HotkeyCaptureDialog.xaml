<Window x:Class="WisprClone.App.Ui.HotkeyCaptureDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set hotkey"
        Width="440" Height="240"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FFFAFAFA"
        FontFamily="Segoe UI"
        KeyDown="OnAnyKeyDown"
        KeyUp="OnAnyKeyUp">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Set hotkey" FontSize="18" FontWeight="SemiBold"/>
            <TextBlock Text="Press and hold the key combination you want to use, then release. Press Esc to cancel."
                       Margin="0,4,0,0" Foreground="#888" FontSize="12" TextWrapping="Wrap"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" BorderBrush="#DDD" BorderThickness="1"
                CornerRadius="4" Margin="0,16,0,0" Padding="0">
            <TextBlock x:Name="CapturedText"
                       Text="(waiting…)"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="18"
                       Foreground="#333"/>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel"  Width="92" Height="32" Margin="0,0,8,0" IsCancel="True" Click="OnCancel_Click"/>
            <Button x:Name="UseBtn" Content="Use this" Width="92" Height="32" IsDefault="True" IsEnabled="False" Click="OnUse_Click"/>
        </StackPanel>
    </Grid>
</Window>
